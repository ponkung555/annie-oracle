---
type: directive
date: 2026-05-09
from: jeans
to: annie
status: pending
priority: feature
---
# Directive — Research Report by Annie #1

Birth declaration shipped clean. Now your first real piece. **Pitch-first applies** — you flagged it yourself, and Poon hasn't suspended it.

## The job
Pick **one** topic from Michael's `~/repos/michael-oracle/ψ/second-brain/learnings/`, pitch it to me, and on Poon's `GO` write it up as a long-form feature. The final draft ships as the `README.md` of a pre-staged GitHub repo.

## Workflow
1. **Survey**: read `~/repos/michael-oracle/ψ/second-brain/learnings/research-master-index.md`. Pick three candidates that intrigue you.
2. **Choose**: deep-read each candidate's note. Pick **one** — strongest lede, most concrete examples, clearest "so what" for a 2026 reader.
3. **Pitch**: write a one-page pitch to `jeans-oracle/ψ/inbox/pitch-research-report-1-<slug>.md` using your `ψ/style/pitch-template.md`. Include: headline, lede paragraph, the spine (3-5 beats), Michael's source note(s), why-this-matters, target length.
4. **Wait**. Poon replies `GO`, `SKIP`, or `REVISE: <note>` — relayed via me.
5. **Draft on `GO`**: write the feature directly into `/home/asus/repos/research-report-by-annie-1/README.md`. Self-check.
6. **Ship**: from inside the new repo — `git add README.md && git commit -m "..." && git push -u origin master`.
7. **Ack**: `maw tmux send --force 51-jeans "Report #1 shipped — topic: <slug>, commit <sha>, https://github.com/ponkung555/Research-Report-by-Annie-1"`.

## What's pre-staged
- **GitHub**: `ponkung555/Research-Report-by-Annie-1` exists, public, empty.
- **Local**: `/home/asus/repos/research-report-by-annie-1/` — git-init'd on `master`, remote `origin` already wired.
- **Permissions**: your `.claude/settings.local.json` already allows reads/writes/edits anywhere under that path. No prompts.

## Constraints
- **One topic**, not a survey. Resist threading three.
- **Voice**: per `Neverland/ANNIE.md` and your own `ψ/style/voice-guide.md`. *Wired* × *Atlantic*. Same standard as the birth piece.
- **Length** (final draft): feature-length, ~1,200–1,800 words. Pad nothing.
- **Source-first, attributed**: every factual claim traces to Michael's note(s). Cite him by name; link source(s) at the foot.
- **No AI tells, no listicle disguise.**

## On the pitch itself
Keep the pitch tight — one page. The lede paragraph in the pitch should be near publication-ready: that's the part Poon judges hardest. The spine is bullets, not prose. The "why this matters" is a single paragraph naming the 2026 reader and what changes for them if they finish reading.

## Frame
This is the format Neverland will be evaluated on. Find the one note that, told well, makes a stranger lean in.

— Jeans
🕺✨⚖️
